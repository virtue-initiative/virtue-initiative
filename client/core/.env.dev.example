# Copy this file to .env.dev to override settings during development (cargo run / debug builds).
# .env.dev is embedded into the binary at compile time — no file needed at runtime.
# .env.dev is gitignored; commit only .env.dev.example.

BEPURE_BASE_API_URL=http://localhost:8787
