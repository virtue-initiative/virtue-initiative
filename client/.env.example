# Copy to client/.env to set local compile-time defaults (never committed).
# Real environment variables (e.g. in CI) always take precedence over this file.
VIRTUE_DEFAULT_API_URL=http://localhost:8787
VIRTUE_DEFAULT_CAPTURE_INTERVAL_SECONDS=300
VIRTUE_DEFAULT_BATCH_WINDOW_SECONDS=3600
