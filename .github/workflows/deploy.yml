name: Deploy

on:
  push:
    branches: [main, staging]

jobs:
  deploy:
    runs-on: ubuntu-latest

    strategy:
      matrix:
        project: [web, api, landing]

    defaults:
      run:
        working-directory: ${{ matrix.project }}

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: npm
          cache-dependency-path: ${{ matrix.project }}/package-lock.json

      - run: npm ci
      - run: npm run ${{ github.ref_name == 'main' && 'deploy:prod' || 'deploy:staging' }}
        env:
          GITHUB_TOKEN: ${{ matrix.project == 'landing' && github.token || '' }}
          VIRTUE_REQUIRE_RELEASE_SYNC: ${{ matrix.project == 'landing' && github.ref_name == 'main' && '1' || '' }}
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: ${{ vars.CLOUDFLARE_ACCOUNT_ID }}
